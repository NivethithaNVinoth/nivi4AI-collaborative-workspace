# Copy to .env and fill in. The platform runs in MOCK mode without a key
# (templated artifacts), and in LIVE mode with a key (real Claude generation).
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6
PORT=4000
